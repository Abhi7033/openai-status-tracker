services:
  - type: worker
    name: openai-status-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m tracker
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
